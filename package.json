{
  "name": "dream-management",
  "version": "0.1.0-p1",
  "description": "Portable nightly memory consolidation for Claude Code agents",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node bin/wrapup-lint.js"
  },
  "bin": {
    "wrapup-lint": "./bin/wrapup-lint.js"
  }
}
