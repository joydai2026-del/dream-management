node_modules/
.DS_Store
*.log
.env
.env.*
# Ignore project-root dream.config.json (consumer-private), but allow
# canonical copies under consumers/<name>/.
/dream.config.json
tests/fixtures/private/
.dream-lock
# Per-repo symlink into the vault project folder; consumer-private.
.vault
